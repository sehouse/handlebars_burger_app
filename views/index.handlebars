<nav class="navbar">
    <img src="assets/img/logo.png" class="d-inline-block align-top mx-auto" alt="">
</nav>
<div class="container my-5">
    <div class="row">
        <div class="col">
            <div class="card">
                <div class="card-header text-center">
                    <h4>New Burgs</h4>
                </div>
                <div class="row-md-9">
                    {{#each burger_data}}
                    {{#unless this.devoured}}
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-9">

                                <pre>{{this.id}}. {{this.burger_name}}</pre>
                            </div>
                            <div class="col-md-3">
                                <form class="devour-form button-size">
                                    <input type="hidden" class="burger_id" type="text" value={{this.id}}>
                                    <button type="submit"
                                        class="btn btn-sm btn-outline-danger m-1 devour-btn float-right">Devour That Burg!<i
                                            class="fas fa-utensils ml-2"></i></button>
                                    </input>

                                </form>
                            </div>
                        </div>
                    </div>
                    {{/unless}}
                    {{/each}}
                </div>
            </div>
        </div>
        <div class="col">
            <div class="card">
                <div class="card-header text-center">
                    <h4>Devoured Burgs!</h4>
                </div>
                <div class="card-body">
                    <div class="col-xs-6 text-center" class="task">
                        {{#each burger_data}}
                        {{#if this.devoured}}
                        <input class="form-control" type="text" placeholder="{{this.id}} . {{this.burger_name}}"
                            readonly>
                        {{/if}}
                        {{/each}}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="row mt-5">
        <div class="col-md-6 offset-md-3">
            <div class="col-xs-12 text-center enter_section">
                <p>What type of burg do you want to submit?</p>
                <form action="/burgers/create" method="POST">
                    <input type="text" class="form-control" name="burger_name" value="" id="enter_text">
                    <button type="submit" class="btn" id="text-enter-button">Enter That Burg!</button>
                    </input>
                </form>
                
            </div>
        </div>
    </div>
</div>